export { pbkdf2, randomBytes } from "node:crypto";

function hashPassword(password: string) {}

function comparePassword(password: string, hash: string, salt: string) {}

export const passwordService = {};
